
# GetSearchEngines200ResponseLimits

搜索结果数量限制

## Properties

Name | Type
------------ | -------------
`_default` | number
`max` | number

## Example

```typescript
import type { GetSearchEngines200ResponseLimits } from 'uapi-browser-sdk-browser'

// TODO: Update the object below with actual values
const example = {
  "_default": 10,
  "max": 100,
} satisfies GetSearchEngines200ResponseLimits

console.log(example)

// Convert the instance to a JSON string
const exampleJSON: string = JSON.stringify(example)
console.log(exampleJSON)

// Parse the JSON string back to an object
const exampleParsed = JSON.parse(exampleJSON) as GetSearchEngines200ResponseLimits
console.log(exampleParsed)
```

[[Back to top]](#) [[Back to API list]](../README.md#api-endpoints) [[Back to Model list]](../README.md#models) [[Back to README]](../README.md)


