/* tslint:disable */
/* eslint-disable */
/**
 * UAPI OpenAPI
 * Generated from UAPI JSON. Code is the interactive documentation: rich descriptions, examples, and x-extensions preserved.
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  GetSaying200Response,
  GetSaying500Response,
  GetSayingRandom200Response,
  GetSayingRandom400Response,
  GetSayingRandom404Response,
} from '../models/index';
import {
    GetSaying200ResponseFromJSON,
    GetSaying200ResponseToJSON,
    GetSaying500ResponseFromJSON,
    GetSaying500ResponseToJSON,
    GetSayingRandom200ResponseFromJSON,
    GetSayingRandom200ResponseToJSON,
    GetSayingRandom400ResponseFromJSON,
    GetSayingRandom400ResponseToJSON,
    GetSayingRandom404ResponseFromJSON,
    GetSayingRandom404ResponseToJSON,
} from '../models/index';

export interface GetSayingRandomRequest {
    mode?: GetSayingRandomModeEnum;
    scene?: GetSayingRandomSceneEnum;
    source?: GetSayingRandomSourceEnum;
    category?: string;
    tag?: string;
}

/**
 * 
 */
export class PoemApi extends runtime.BaseAPI {

    /**
     * 想在你的应用里每天展示一句不一样的话，给用户一点小小的惊喜吗？这个“一言”接口就是为此而生。  ## 功能概述 每次调用，它都会从我们精心收集的、包含数千条诗词、动漫台词、名人名言的语料库中，随机返回一条。你可以用它来做网站首页的Slogan、应用的启动语，或者任何需要灵感点缀的地方。
     * 一言
     */
    async getSayingRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<GetSaying200Response>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};


        let urlPath = `/saying`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => GetSaying200ResponseFromJSON(jsonValue));
    }

    /**
     * 想在你的应用里每天展示一句不一样的话，给用户一点小小的惊喜吗？这个“一言”接口就是为此而生。  ## 功能概述 每次调用，它都会从我们精心收集的、包含数千条诗词、动漫台词、名人名言的语料库中，随机返回一条。你可以用它来做网站首页的Slogan、应用的启动语，或者任何需要灵感点缀的地方。
     * 一言
     */
    async getSaying(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<GetSaying200Response> {
        const response = await this.getSayingRaw(initOverrides);
        return await response.value();
    }

    /**
     * 一言接口，返回一条随机语录。通过 `mode` 参数切换四种返回方式，并支持按来源、分类、标签过滤。  ## 四种模式（`mode`） - **`random`（默认）**：每次调用随机返回一条语录。 - **`daily`**：同一天内返回固定的同一条，适合每日打卡、签到等场景。 - **`recommend`**：配合 `scene` 参数，返回指定场景（如 `night`、`morning`）的语录。 - **`moment`**：根据请求时所处时段，自动返回应景语录。  ## 语言控制 语料分中英文两类，可通过 `source` 或 `category` 控制： - 需要中文：`source` 选「综合句子语料库 / 曹星宇句子集」，或 `category` 选中文分类（如 影视、文学、诗词）。 - 需要英文：`source` 选「Quotable / 英文历史名言」。  ## 使用须知 > [!NOTE] > - `source`、`category`、`tag` 支持多值，用英文逗号 `,` 或分号 `;` 分隔。 > - `scene` 仅在 `mode=recommend` 时生效且必填，其他模式下会被忽略。 > - 请求示例： >   - 随机：`GET /api/v1/saying/random` >   - 每日：`GET /api/v1/saying/random?mode=daily` >   - 场景：`GET /api/v1/saying/random?mode=recommend&scene=night` >   - 此刻：`GET /api/v1/saying/random?mode=moment`
     * 一言（随机/每日/场景/此刻）
     */
    async getSayingRandomRaw(requestParameters: GetSayingRandomRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<GetSayingRandom200Response>> {
        const queryParameters: any = {};

        if (requestParameters['mode'] != null) {
            queryParameters['mode'] = requestParameters['mode'];
        }

        if (requestParameters['scene'] != null) {
            queryParameters['scene'] = requestParameters['scene'];
        }

        if (requestParameters['source'] != null) {
            queryParameters['source'] = requestParameters['source'];
        }

        if (requestParameters['category'] != null) {
            queryParameters['category'] = requestParameters['category'];
        }

        if (requestParameters['tag'] != null) {
            queryParameters['tag'] = requestParameters['tag'];
        }

        const headerParameters: runtime.HTTPHeaders = {};


        let urlPath = `/saying/random`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => GetSayingRandom200ResponseFromJSON(jsonValue));
    }

    /**
     * 一言接口，返回一条随机语录。通过 `mode` 参数切换四种返回方式，并支持按来源、分类、标签过滤。  ## 四种模式（`mode`） - **`random`（默认）**：每次调用随机返回一条语录。 - **`daily`**：同一天内返回固定的同一条，适合每日打卡、签到等场景。 - **`recommend`**：配合 `scene` 参数，返回指定场景（如 `night`、`morning`）的语录。 - **`moment`**：根据请求时所处时段，自动返回应景语录。  ## 语言控制 语料分中英文两类，可通过 `source` 或 `category` 控制： - 需要中文：`source` 选「综合句子语料库 / 曹星宇句子集」，或 `category` 选中文分类（如 影视、文学、诗词）。 - 需要英文：`source` 选「Quotable / 英文历史名言」。  ## 使用须知 > [!NOTE] > - `source`、`category`、`tag` 支持多值，用英文逗号 `,` 或分号 `;` 分隔。 > - `scene` 仅在 `mode=recommend` 时生效且必填，其他模式下会被忽略。 > - 请求示例： >   - 随机：`GET /api/v1/saying/random` >   - 每日：`GET /api/v1/saying/random?mode=daily` >   - 场景：`GET /api/v1/saying/random?mode=recommend&scene=night` >   - 此刻：`GET /api/v1/saying/random?mode=moment`
     * 一言（随机/每日/场景/此刻）
     */
    async getSayingRandom(requestParameters: GetSayingRandomRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<GetSayingRandom200Response> {
        const response = await this.getSayingRandomRaw(requestParameters, initOverrides);
        return await response.value();
    }

}

/**
 * @export
 */
export const GetSayingRandomModeEnum = {
    Random: 'random',
    Daily: 'daily',
    Recommend: 'recommend',
    Moment: 'moment'
} as const;
export type GetSayingRandomModeEnum = typeof GetSayingRandomModeEnum[keyof typeof GetSayingRandomModeEnum];
/**
 * @export
 */
export const GetSayingRandomSceneEnum = {
    Dawn: 'dawn',
    Morning: 'morning',
    Noon: 'noon',
    Afternoon: 'afternoon',
    Evening: 'evening',
    Night: 'night',
    DeepNight: 'deep-night',
    Work: 'work',
    Coding: 'coding',
    Meeting: 'meeting',
    Relax: 'relax',
    Emo: 'emo',
    Philosophy: 'philosophy'
} as const;
export type GetSayingRandomSceneEnum = typeof GetSayingRandomSceneEnum[keyof typeof GetSayingRandomSceneEnum];
/**
 * @export
 */
export const GetSayingRandomSourceEnum = {
    CaoxingyuSentence: 'caoxingyu sentence',
    EnglishHistoricalQuotes: 'english historical quotes',
    Quotable: 'quotable',
    SentencesBundle: 'sentences bundle'
} as const;
export type GetSayingRandomSourceEnum = typeof GetSayingRandomSourceEnum[keyof typeof GetSayingRandomSourceEnum];
