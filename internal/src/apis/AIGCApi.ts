/* tslint:disable */
/* eslint-disable */
/**
 * UAPI OpenAPI
 * Generated from UAPI JSON. Code is the interactive documentation: rich descriptions, examples, and x-extensions preserved.
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  PostWatermarkDecode200Response,
  PostWatermarkDecode400Response,
  PostWatermarkDecode503Response,
  PostWatermarkEmbed200Response,
  PostWatermarkEmbed413Response,
  PostWatermarkLabel200Response,
  PostWatermarkLabel400Response,
  PostWatermarkProducerCode200Response,
  PostWatermarkProducerCode400Response,
  PostWatermarkProducerCodeRequest,
} from '../models/index';
import {
    PostWatermarkDecode200ResponseFromJSON,
    PostWatermarkDecode200ResponseToJSON,
    PostWatermarkDecode400ResponseFromJSON,
    PostWatermarkDecode400ResponseToJSON,
    PostWatermarkDecode503ResponseFromJSON,
    PostWatermarkDecode503ResponseToJSON,
    PostWatermarkEmbed200ResponseFromJSON,
    PostWatermarkEmbed200ResponseToJSON,
    PostWatermarkEmbed413ResponseFromJSON,
    PostWatermarkEmbed413ResponseToJSON,
    PostWatermarkLabel200ResponseFromJSON,
    PostWatermarkLabel200ResponseToJSON,
    PostWatermarkLabel400ResponseFromJSON,
    PostWatermarkLabel400ResponseToJSON,
    PostWatermarkProducerCode200ResponseFromJSON,
    PostWatermarkProducerCode200ResponseToJSON,
    PostWatermarkProducerCode400ResponseFromJSON,
    PostWatermarkProducerCode400ResponseToJSON,
    PostWatermarkProducerCodeRequestFromJSON,
    PostWatermarkProducerCodeRequestToJSON,
} from '../models/index';

export interface PostWatermarkDecodeRequest {
    ecc?: PostWatermarkDecodeEccEnum;
    file?: Blob;
    imageBase64?: string;
    modelType?: PostWatermarkDecodeModelTypeEnum;
    url?: string;
}

export interface PostWatermarkEmbedRequest {
    payload: string;
    ecc?: PostWatermarkEmbedEccEnum;
    file?: Blob;
    imageBase64?: string;
    jpegQuality?: number;
    modelType?: PostWatermarkEmbedModelTypeEnum;
    outFormat?: PostWatermarkEmbedOutFormatEnum;
    strength?: number;
    url?: string;
}

export interface PostWatermarkLabelRequest {
    contentProducer: string;
    contentPropagator?: string;
    embedWatermark?: boolean;
    explicitHeightRatio?: number;
    explicitLabel?: boolean;
    explicitPosition?: PostWatermarkLabelExplicitPositionEnum;
    explicitText?: string;
    file?: Blob;
    imageBase64?: string;
    jpegQuality?: number;
    label?: PostWatermarkLabelLabelEnum;
    outFormat?: PostWatermarkLabelOutFormatEnum;
    produceId?: string;
    propagateId?: string;
    skipMetadata?: boolean;
    url?: string;
    watermarkPayload?: string;
}

export interface PostWatermarkProducerCodeOperationRequest {
    postWatermarkProducerCodeRequest: PostWatermarkProducerCodeRequest;
}

/**
 * 
 */
export class AIGCApi extends runtime.BaseAPI {

    /**
     * 遇到一张疑似被盗用或 AI 生成的图片，想查查它有没有被打过“思想钢印”？直接把图片扔给这个接口，它能把藏在里面的标识完完整整地提取出来。  ## 功能概述 此接口用于检测图片中是否包含隐形水印，若存在则还原出写入的原始标识内容。即使图片经历过压缩、截取或多次网络转发，也具备较高的成功提取率。接口具备防误判机制，若图片确实未经过处理，会明确返回未检测到结果，而不会强制拼接无效内容。  ## 使用须知 如果您在嵌入水印时修改过进阶参数（如 `ecc` 或 `model_type`），在提取时必须传入相同的参数值。若嵌入时使用的是默认配置，此处直接留空即可。
     * 提取图片隐水印
     */
    async postWatermarkDecodeRaw(requestParameters: PostWatermarkDecodeRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PostWatermarkDecode200Response>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const consumes: runtime.Consume[] = [
            { contentType: 'multipart/form-data' },
        ];
        // @ts-ignore: canConsumeForm may be unused
        const canConsumeForm = runtime.canConsumeForm(consumes);

        let formParams: { append(param: string, value: any): any };
        let useForm = false;
        // use FormData to transmit files using content-type "multipart/form-data"
        useForm = canConsumeForm;
        if (useForm) {
            formParams = new FormData();
        } else {
            formParams = new URLSearchParams();
        }

        if (requestParameters['ecc'] != null) {
            formParams.append('ecc', requestParameters['ecc'] as any);
        }

        if (requestParameters['file'] != null) {
            formParams.append('file', requestParameters['file'] as any);
        }

        if (requestParameters['imageBase64'] != null) {
            formParams.append('image_base64', requestParameters['imageBase64'] as any);
        }

        if (requestParameters['modelType'] != null) {
            formParams.append('model_type', requestParameters['modelType'] as any);
        }

        if (requestParameters['url'] != null) {
            formParams.append('url', requestParameters['url'] as any);
        }


        let urlPath = `/watermark/decode`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: formParams,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PostWatermarkDecode200ResponseFromJSON(jsonValue));
    }

    /**
     * 遇到一张疑似被盗用或 AI 生成的图片，想查查它有没有被打过“思想钢印”？直接把图片扔给这个接口，它能把藏在里面的标识完完整整地提取出来。  ## 功能概述 此接口用于检测图片中是否包含隐形水印，若存在则还原出写入的原始标识内容。即使图片经历过压缩、截取或多次网络转发，也具备较高的成功提取率。接口具备防误判机制，若图片确实未经过处理，会明确返回未检测到结果，而不会强制拼接无效内容。  ## 使用须知 如果您在嵌入水印时修改过进阶参数（如 `ecc` 或 `model_type`），在提取时必须传入相同的参数值。若嵌入时使用的是默认配置，此处直接留空即可。
     * 提取图片隐水印
     */
    async postWatermarkDecode(requestParameters: PostWatermarkDecodeRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PostWatermarkDecode200Response> {
        const response = await this.postWatermarkDecodeRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * 想给自己的原创图片打上专属烙印，又不想破坏画面美感？或者是为了 AIGC 生成的图片做后续溯源追踪？这个接口能帮您把自定义标识悄悄藏进图片里。  ## 功能概述 您可以上传图片并指定一段标识文本，接口会利用算法将这段文本隐形嵌入到图片的像素之中。嵌入后的图片在肉眼看来毫无变化，但能够有效抵抗常见的缩放、裁剪、社交平台压缩与二次转发。适合用于标记图片来源、追踪分发渠道或进行隐蔽的版权确权。  ## 使用须知 **容量限制**：受限于隐形水印的算法特性，图片能嵌入的字符长度有限（通常为短码）。建议只放入简短的溯源 ID 或特征码，将完整的映射信息存储在您自己的数据库中。  **参数说明**：提供图片的方式（`file` / `url` / `image_base64`）三选一即可；其他进阶参数如无特殊需求，建议保持留空使用默认最佳配置。
     * 添加图片隐水印
     */
    async postWatermarkEmbedRaw(requestParameters: PostWatermarkEmbedRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PostWatermarkEmbed200Response>> {
        if (requestParameters['payload'] == null) {
            throw new runtime.RequiredError(
                'payload',
                'Required parameter "payload" was null or undefined when calling postWatermarkEmbed().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const consumes: runtime.Consume[] = [
            { contentType: 'multipart/form-data' },
        ];
        // @ts-ignore: canConsumeForm may be unused
        const canConsumeForm = runtime.canConsumeForm(consumes);

        let formParams: { append(param: string, value: any): any };
        let useForm = false;
        // use FormData to transmit files using content-type "multipart/form-data"
        useForm = canConsumeForm;
        if (useForm) {
            formParams = new FormData();
        } else {
            formParams = new URLSearchParams();
        }

        if (requestParameters['ecc'] != null) {
            formParams.append('ecc', requestParameters['ecc'] as any);
        }

        if (requestParameters['file'] != null) {
            formParams.append('file', requestParameters['file'] as any);
        }

        if (requestParameters['imageBase64'] != null) {
            formParams.append('image_base64', requestParameters['imageBase64'] as any);
        }

        if (requestParameters['jpegQuality'] != null) {
            formParams.append('jpeg_quality', requestParameters['jpegQuality'] as any);
        }

        if (requestParameters['modelType'] != null) {
            formParams.append('model_type', requestParameters['modelType'] as any);
        }

        if (requestParameters['outFormat'] != null) {
            formParams.append('out_format', requestParameters['outFormat'] as any);
        }

        if (requestParameters['payload'] != null) {
            formParams.append('payload', requestParameters['payload'] as any);
        }

        if (requestParameters['strength'] != null) {
            formParams.append('strength', requestParameters['strength'] as any);
        }

        if (requestParameters['url'] != null) {
            formParams.append('url', requestParameters['url'] as any);
        }


        let urlPath = `/watermark/embed`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: formParams,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PostWatermarkEmbed200ResponseFromJSON(jsonValue));
    }

    /**
     * 想给自己的原创图片打上专属烙印，又不想破坏画面美感？或者是为了 AIGC 生成的图片做后续溯源追踪？这个接口能帮您把自定义标识悄悄藏进图片里。  ## 功能概述 您可以上传图片并指定一段标识文本，接口会利用算法将这段文本隐形嵌入到图片的像素之中。嵌入后的图片在肉眼看来毫无变化，但能够有效抵抗常见的缩放、裁剪、社交平台压缩与二次转发。适合用于标记图片来源、追踪分发渠道或进行隐蔽的版权确权。  ## 使用须知 **容量限制**：受限于隐形水印的算法特性，图片能嵌入的字符长度有限（通常为短码）。建议只放入简短的溯源 ID 或特征码，将完整的映射信息存储在您自己的数据库中。  **参数说明**：提供图片的方式（`file` / `url` / `image_base64`）三选一即可；其他进阶参数如无特殊需求，建议保持留空使用默认最佳配置。
     * 添加图片隐水印
     */
    async postWatermarkEmbed(requestParameters: PostWatermarkEmbedRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PostWatermarkEmbed200Response> {
        const response = await this.postWatermarkEmbedRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * AI 生成的图片上线前需要满足国标合规要求？别头疼，调用一次这个接口，就能自动给图片打上符合《GB 45438-2025》标准的三层标识。  ## 三层标识说明 此接口一次性支持注入以下三种符合国标规范的标识： - **元数据隐式标识**（默认开启）：在图片文件的 EXIF/XMP 元数据中记录此内容的“AI 生成”属性及服务提供者信息，不影响视觉呈现。 - **可见角标标识**（可选）：在图片指定角落叠加醒目的“AI 生成”文字，字号会自动计算以符合国标关于“字符高度不小于画面短边 5%”的规定。 - **隐形水印标识**（可选）：在图像像素深层嵌入抗压缩溯源信息。  ## 使用须知 **必填说明**：根据规范，`content_producer`（服务提供者编码）为必填项。推荐使用本平台配套的 `/watermark/producer-code` 接口快速生成规范的 27 位服务提供者编码。  **标识组合**：若您需要关闭默认的元数据标识（`skip_metadata=true`），则必须至少开启可见角标或隐形水印中的一项，以保证图片拥有合规标识。
     * 添加 AI 生成内容标识
     */
    async postWatermarkLabelRaw(requestParameters: PostWatermarkLabelRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PostWatermarkLabel200Response>> {
        if (requestParameters['contentProducer'] == null) {
            throw new runtime.RequiredError(
                'contentProducer',
                'Required parameter "contentProducer" was null or undefined when calling postWatermarkLabel().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const consumes: runtime.Consume[] = [
            { contentType: 'multipart/form-data' },
        ];
        // @ts-ignore: canConsumeForm may be unused
        const canConsumeForm = runtime.canConsumeForm(consumes);

        let formParams: { append(param: string, value: any): any };
        let useForm = false;
        // use FormData to transmit files using content-type "multipart/form-data"
        useForm = canConsumeForm;
        if (useForm) {
            formParams = new FormData();
        } else {
            formParams = new URLSearchParams();
        }

        if (requestParameters['contentProducer'] != null) {
            formParams.append('content_producer', requestParameters['contentProducer'] as any);
        }

        if (requestParameters['contentPropagator'] != null) {
            formParams.append('content_propagator', requestParameters['contentPropagator'] as any);
        }

        if (requestParameters['embedWatermark'] != null) {
            formParams.append('embed_watermark', requestParameters['embedWatermark'] as any);
        }

        if (requestParameters['explicitHeightRatio'] != null) {
            formParams.append('explicit_height_ratio', requestParameters['explicitHeightRatio'] as any);
        }

        if (requestParameters['explicitLabel'] != null) {
            formParams.append('explicit_label', requestParameters['explicitLabel'] as any);
        }

        if (requestParameters['explicitPosition'] != null) {
            formParams.append('explicit_position', requestParameters['explicitPosition'] as any);
        }

        if (requestParameters['explicitText'] != null) {
            formParams.append('explicit_text', requestParameters['explicitText'] as any);
        }

        if (requestParameters['file'] != null) {
            formParams.append('file', requestParameters['file'] as any);
        }

        if (requestParameters['imageBase64'] != null) {
            formParams.append('image_base64', requestParameters['imageBase64'] as any);
        }

        if (requestParameters['jpegQuality'] != null) {
            formParams.append('jpeg_quality', requestParameters['jpegQuality'] as any);
        }

        if (requestParameters['label'] != null) {
            formParams.append('label', requestParameters['label'] as any);
        }

        if (requestParameters['outFormat'] != null) {
            formParams.append('out_format', requestParameters['outFormat'] as any);
        }

        if (requestParameters['produceId'] != null) {
            formParams.append('produce_id', requestParameters['produceId'] as any);
        }

        if (requestParameters['propagateId'] != null) {
            formParams.append('propagate_id', requestParameters['propagateId'] as any);
        }

        if (requestParameters['skipMetadata'] != null) {
            formParams.append('skip_metadata', requestParameters['skipMetadata'] as any);
        }

        if (requestParameters['url'] != null) {
            formParams.append('url', requestParameters['url'] as any);
        }

        if (requestParameters['watermarkPayload'] != null) {
            formParams.append('watermark_payload', requestParameters['watermarkPayload'] as any);
        }


        let urlPath = `/watermark/label`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: formParams,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PostWatermarkLabel200ResponseFromJSON(jsonValue));
    }

    /**
     * AI 生成的图片上线前需要满足国标合规要求？别头疼，调用一次这个接口，就能自动给图片打上符合《GB 45438-2025》标准的三层标识。  ## 三层标识说明 此接口一次性支持注入以下三种符合国标规范的标识： - **元数据隐式标识**（默认开启）：在图片文件的 EXIF/XMP 元数据中记录此内容的“AI 生成”属性及服务提供者信息，不影响视觉呈现。 - **可见角标标识**（可选）：在图片指定角落叠加醒目的“AI 生成”文字，字号会自动计算以符合国标关于“字符高度不小于画面短边 5%”的规定。 - **隐形水印标识**（可选）：在图像像素深层嵌入抗压缩溯源信息。  ## 使用须知 **必填说明**：根据规范，`content_producer`（服务提供者编码）为必填项。推荐使用本平台配套的 `/watermark/producer-code` 接口快速生成规范的 27 位服务提供者编码。  **标识组合**：若您需要关闭默认的元数据标识（`skip_metadata=true`），则必须至少开启可见角标或隐形水印中的一项，以保证图片拥有合规标识。
     * 添加 AI 生成内容标识
     */
    async postWatermarkLabel(requestParameters: PostWatermarkLabelRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PostWatermarkLabel200Response> {
        const response = await this.postWatermarkLabelRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * 还在发愁怎么拼出符合《GB 45438-2025》要求的 27 位 AIGC 服务提供者编码？无论是想一键生成自己的专属合规编码，还是想校验已有编码对不对，这个接口都能帮您轻松搞定。  ## 功能概述 此接口具备“生成”与“校验”双重能力： - **生成模式**：填入组织或个人的主体身份信息与证件号，接口会自动处理复杂的拼位规则，输出标准的 27 位编码。 - **校验模式**：仅需填入现成的 `code`，接口将逐段拆解、验证其合法性，并解析出各个组成部分的实际含义。  ## 使用须知 **模式互斥**：当您在请求体中填入了 `code` 参数时，接口会自动进入“校验模式”，其余的生成参数将被忽略；反之则进入“生成模式”。  **独立生成**：该编码不依赖向外部机构注册申请，而是基于您的法定身份标识（如企业统一社会信用代码、个人身份证等）根据标准算法独立生成即可。
     * 生成 AIGC 服务提供者编码
     */
    async postWatermarkProducerCodeRaw(requestParameters: PostWatermarkProducerCodeOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PostWatermarkProducerCode200Response>> {
        if (requestParameters['postWatermarkProducerCodeRequest'] == null) {
            throw new runtime.RequiredError(
                'postWatermarkProducerCodeRequest',
                'Required parameter "postWatermarkProducerCodeRequest" was null or undefined when calling postWatermarkProducerCode().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/watermark/producer-code`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: PostWatermarkProducerCodeRequestToJSON(requestParameters['postWatermarkProducerCodeRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PostWatermarkProducerCode200ResponseFromJSON(jsonValue));
    }

    /**
     * 还在发愁怎么拼出符合《GB 45438-2025》要求的 27 位 AIGC 服务提供者编码？无论是想一键生成自己的专属合规编码，还是想校验已有编码对不对，这个接口都能帮您轻松搞定。  ## 功能概述 此接口具备“生成”与“校验”双重能力： - **生成模式**：填入组织或个人的主体身份信息与证件号，接口会自动处理复杂的拼位规则，输出标准的 27 位编码。 - **校验模式**：仅需填入现成的 `code`，接口将逐段拆解、验证其合法性，并解析出各个组成部分的实际含义。  ## 使用须知 **模式互斥**：当您在请求体中填入了 `code` 参数时，接口会自动进入“校验模式”，其余的生成参数将被忽略；反之则进入“生成模式”。  **独立生成**：该编码不依赖向外部机构注册申请，而是基于您的法定身份标识（如企业统一社会信用代码、个人身份证等）根据标准算法独立生成即可。
     * 生成 AIGC 服务提供者编码
     */
    async postWatermarkProducerCode(requestParameters: PostWatermarkProducerCodeOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PostWatermarkProducerCode200Response> {
        const response = await this.postWatermarkProducerCodeRaw(requestParameters, initOverrides);
        return await response.value();
    }

}

/**
 * @export
 */
export const PostWatermarkDecodeEccEnum = {
    BchSuper: 'BCH_SUPER',
    Bch5: 'BCH_5',
    Bch4: 'BCH_4',
    Bch3: 'BCH_3'
} as const;
export type PostWatermarkDecodeEccEnum = typeof PostWatermarkDecodeEccEnum[keyof typeof PostWatermarkDecodeEccEnum];
/**
 * @export
 */
export const PostWatermarkDecodeModelTypeEnum = {
    B: 'B',
    Q: 'Q'
} as const;
export type PostWatermarkDecodeModelTypeEnum = typeof PostWatermarkDecodeModelTypeEnum[keyof typeof PostWatermarkDecodeModelTypeEnum];
/**
 * @export
 */
export const PostWatermarkEmbedEccEnum = {
    BchSuper: 'BCH_SUPER',
    Bch5: 'BCH_5',
    Bch4: 'BCH_4',
    Bch3: 'BCH_3'
} as const;
export type PostWatermarkEmbedEccEnum = typeof PostWatermarkEmbedEccEnum[keyof typeof PostWatermarkEmbedEccEnum];
/**
 * @export
 */
export const PostWatermarkEmbedModelTypeEnum = {
    B: 'B',
    Q: 'Q'
} as const;
export type PostWatermarkEmbedModelTypeEnum = typeof PostWatermarkEmbedModelTypeEnum[keyof typeof PostWatermarkEmbedModelTypeEnum];
/**
 * @export
 */
export const PostWatermarkEmbedOutFormatEnum = {
    Png: 'png',
    Jpeg: 'jpeg'
} as const;
export type PostWatermarkEmbedOutFormatEnum = typeof PostWatermarkEmbedOutFormatEnum[keyof typeof PostWatermarkEmbedOutFormatEnum];
/**
 * @export
 */
export const PostWatermarkLabelExplicitPositionEnum = {
    TopLeft: 'top-left',
    TopRight: 'top-right',
    BottomLeft: 'bottom-left',
    BottomRight: 'bottom-right'
} as const;
export type PostWatermarkLabelExplicitPositionEnum = typeof PostWatermarkLabelExplicitPositionEnum[keyof typeof PostWatermarkLabelExplicitPositionEnum];
/**
 * @export
 */
export const PostWatermarkLabelLabelEnum = {
    _1: '1',
    _2: '2',
    _3: '3'
} as const;
export type PostWatermarkLabelLabelEnum = typeof PostWatermarkLabelLabelEnum[keyof typeof PostWatermarkLabelLabelEnum];
/**
 * @export
 */
export const PostWatermarkLabelOutFormatEnum = {
    Png: 'png',
    Jpeg: 'jpeg'
} as const;
export type PostWatermarkLabelOutFormatEnum = typeof PostWatermarkLabelOutFormatEnum[keyof typeof PostWatermarkLabelOutFormatEnum];
