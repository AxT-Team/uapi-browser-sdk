/* tslint:disable */
/* eslint-disable */
/**
 * UAPI OpenAPI
 * Generated from UAPI JSON. Code is the interactive documentation: rich descriptions, examples, and x-extensions preserved.
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  GetDailyNewsImage500Response,
  GetDailyNewsImage502Response,
  GetDailyWord200Response,
} from '../models/index';
import {
    GetDailyNewsImage500ResponseFromJSON,
    GetDailyNewsImage500ResponseToJSON,
    GetDailyNewsImage502ResponseFromJSON,
    GetDailyNewsImage502ResponseToJSON,
    GetDailyWord200ResponseFromJSON,
    GetDailyWord200ResponseToJSON,
} from '../models/index';

export interface GetDailyWordRequest {
    lang?: GetDailyWordLangEnum;
    category?: GetDailyWordCategoryEnum;
    count?: number;
    date?: string;
    seed?: number;
    example?: boolean;
    phonetic?: boolean;
    define?: boolean;
}

/**
 * 
 */
export class DailyApi extends runtime.BaseAPI {

    /**
     * 想用一张图快速了解天下大事？这个接口为你一键生成今日新闻摘要，非常适合用在早报、数字看板或应用首页等场景。  ## 功能概述 此接口会实时抓取各大平台的热点新闻，并动态地将它们渲染成一张清晰、美观的摘要图片。你调用接口，直接就能得到一张可以展示的图片。  ## 使用须知 调用此接口时，请务必注意以下两点：  1.  **响应格式是图片**：接口成功时直接返回 `image/jpeg` 格式的二进制数据，而非 JSON。请确保你的客户端能正确处理二进制流，例如直接在 `<img>` 标签中显示，或保存为 `.jpg` 文件。  2.  **设置合理超时**：由于涉及实时新闻抓取和图片渲染，处理过程可能耗时数秒。建议将客户端请求超时时间设置为至少10秒，以防止因等待过久而失败。  > [!IMPORTANT] > 未能正确处理图片响应或超时设置过短，是导致调用失败的最常见原因。
     * 每日新闻图
     */
    async getDailyNewsImageRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Blob>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};


        let urlPath = `/daily/news-image`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.BlobApiResponse(response);
    }

    /**
     * 想用一张图快速了解天下大事？这个接口为你一键生成今日新闻摘要，非常适合用在早报、数字看板或应用首页等场景。  ## 功能概述 此接口会实时抓取各大平台的热点新闻，并动态地将它们渲染成一张清晰、美观的摘要图片。你调用接口，直接就能得到一张可以展示的图片。  ## 使用须知 调用此接口时，请务必注意以下两点：  1.  **响应格式是图片**：接口成功时直接返回 `image/jpeg` 格式的二进制数据，而非 JSON。请确保你的客户端能正确处理二进制流，例如直接在 `<img>` 标签中显示，或保存为 `.jpg` 文件。  2.  **设置合理超时**：由于涉及实时新闻抓取和图片渲染，处理过程可能耗时数秒。建议将客户端请求超时时间设置为至少10秒，以防止因等待过久而失败。  > [!IMPORTANT] > 未能正确处理图片响应或超时设置过短，是导致调用失败的最常见原因。
     * 每日新闻图
     */
    async getDailyNewsImage(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Blob> {
        const response = await this.getDailyNewsImageRaw(initOverrides);
        return await response.value();
    }

    /**
     * 想给你的学习打卡、英语小组件或机器人推送加一个『每日单词』？这个接口每天给你一个稳定的单词，同一天多次请求结果一致。  ## 功能概述 默认返回 1 个英文单词，支持按词库范围筛选，可选择是否附带例句和音标。也可以用 `count` 一次返回多个词，用于词汇复习列表。  ## 词库选项 - `all`：全部英文词库，适合通用每日推荐。 - `cet4`：大学英语四级词汇，适合基础复习。 - `cet6`：大学英语六级词汇，适合进阶复习。 - `ielts`：雅思词汇，适合留学考试准备。 - `toefl`：托福词汇，适合北美考试准备。 - `gre`：GRE 词汇，适合高阶词汇训练。  ## 使用须知 > [!IMPORTANT] > `date` 与 `seed` 用于复现某一天或某个固定的取词结果，二者不能同时传入。
     * 每日单词
     */
    async getDailyWordRaw(requestParameters: GetDailyWordRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<GetDailyWord200Response>> {
        const queryParameters: any = {};

        if (requestParameters['lang'] != null) {
            queryParameters['lang'] = requestParameters['lang'];
        }

        if (requestParameters['category'] != null) {
            queryParameters['category'] = requestParameters['category'];
        }

        if (requestParameters['count'] != null) {
            queryParameters['count'] = requestParameters['count'];
        }

        if (requestParameters['date'] != null) {
            queryParameters['date'] = requestParameters['date'];
        }

        if (requestParameters['seed'] != null) {
            queryParameters['seed'] = requestParameters['seed'];
        }

        if (requestParameters['example'] != null) {
            queryParameters['example'] = requestParameters['example'];
        }

        if (requestParameters['phonetic'] != null) {
            queryParameters['phonetic'] = requestParameters['phonetic'];
        }

        if (requestParameters['define'] != null) {
            queryParameters['define'] = requestParameters['define'];
        }

        const headerParameters: runtime.HTTPHeaders = {};


        let urlPath = `/daily/word`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => GetDailyWord200ResponseFromJSON(jsonValue));
    }

    /**
     * 想给你的学习打卡、英语小组件或机器人推送加一个『每日单词』？这个接口每天给你一个稳定的单词，同一天多次请求结果一致。  ## 功能概述 默认返回 1 个英文单词，支持按词库范围筛选，可选择是否附带例句和音标。也可以用 `count` 一次返回多个词，用于词汇复习列表。  ## 词库选项 - `all`：全部英文词库，适合通用每日推荐。 - `cet4`：大学英语四级词汇，适合基础复习。 - `cet6`：大学英语六级词汇，适合进阶复习。 - `ielts`：雅思词汇，适合留学考试准备。 - `toefl`：托福词汇，适合北美考试准备。 - `gre`：GRE 词汇，适合高阶词汇训练。  ## 使用须知 > [!IMPORTANT] > `date` 与 `seed` 用于复现某一天或某个固定的取词结果，二者不能同时传入。
     * 每日单词
     */
    async getDailyWord(requestParameters: GetDailyWordRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<GetDailyWord200Response> {
        const response = await this.getDailyWordRaw(requestParameters, initOverrides);
        return await response.value();
    }

}

/**
 * @export
 */
export const GetDailyWordLangEnum = {
    En: 'en'
} as const;
export type GetDailyWordLangEnum = typeof GetDailyWordLangEnum[keyof typeof GetDailyWordLangEnum];
/**
 * @export
 */
export const GetDailyWordCategoryEnum = {
    All: 'all',
    Cet4: 'cet4',
    Cet6: 'cet6',
    Ielts: 'ielts',
    Toefl: 'toefl',
    Gre: 'gre'
} as const;
export type GetDailyWordCategoryEnum = typeof GetDailyWordCategoryEnum[keyof typeof GetDailyWordCategoryEnum];
